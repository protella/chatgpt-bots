from setuptools import setup, find_packages

setup(
    name="chatgpt-bots",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Dependencies are already listed in requirements.txt
        # and installed separately in Dockerfile
    ],
) 