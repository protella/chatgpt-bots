# Core dependencies
python-dotenv>=1.0.0
openai>=2.45.0  # GPT-5.6 family support (max effort literal, prompt_cache_options)
slack_bolt>=1.27.0
slack_sdk>=3.43.0  # imported directly (WebClient, SlackApiError); do not rely on slack_bolt's transitive pin
Pillow>=11.0.0
croniter>=6.0.0
pytz>=2024.1

# Logging dependencies
concurrent-log-handler>=0.9.28

# Async dependencies
aiosqlite>=0.20.0
aiohttp>=3.10.0

# Testing dependencies
pytest>=8.0.0
pytest-asyncio>=1.0.0
pytest-cov>=7.1.0
pytest-mock>=3.14.0
pytest-env>=1.0.0
coverage[toml]>=7.4.0

# Document processing dependencies
beautifulsoup4>=4.12.0  # Parse Slack canvas HTML back into markdown (canvas_tools); hard dependency
pypdf>=5.0.0
pdfplumber>=0.11.0
python-docx>=1.0.0
python-pptx>=1.0.0
openpyxl>=3.1.5
pandas>=2.2.0
tabulate>=0.10.0
pdf2image>=1.17.0  # For converting PDF pages to images (requires poppler-utils)
pytesseract>=0.3.13  # OCR text from scanned PDFs (requires tesseract-ocr system package)
striprtf>=0.0.29  # Strip RTF control codes to plain text (.rtf); BSD-3-licensed, pure-Python
